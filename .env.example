# StellaSentinal Environment Configuration
# Copy to .env and adjust values for your environment.

# Base workspace path (defaults to repo root if unset)
# STELLA_WORKSPACE=/path/to/StellaSentinal

# Daemon pulse interval in seconds (default: 60)
# STELLA_PULSE_INTERVAL=60

# Subprocess timeout in seconds (default: 120)
# STELLA_SUBPROCESS_TIMEOUT=120

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# STELLA_LOG_LEVEL=INFO
