# =============================================================================
# Stella Sentinel - Minimal Configuration
# =============================================================================
# Copy to .env to get started quickly. Only essential variables are included.
# For advanced options (ML tuning, ingestion controls, etc.), see env.template
# =============================================================================

# -----------------------------------------------------------------------------
# Required: PostgreSQL Backend (anomalies, baselines, results)
# -----------------------------------------------------------------------------
BACKEND_DB_PASS=changeme

# -----------------------------------------------------------------------------
# Required: SOTI XSight SQL Server (source telemetry data)
# -----------------------------------------------------------------------------
# For Docker: use host.docker.internal (Mac/Windows) or your host IP (Linux)
DW_DB_HOST=your-server.example.com
DW_DB_NAME=XSight
DW_DB_USER=your-username
DW_DB_PASS=your-password

# -----------------------------------------------------------------------------
# Optional: SOTI MobiControl SQL Server (device inventory enrichment)
# -----------------------------------------------------------------------------
# Leave blank if not using MobiControl
MC_DB_HOST=
MC_DB_USER=
MC_DB_PASS=

# -----------------------------------------------------------------------------
# That's it! Run: make up
# -----------------------------------------------------------------------------
# The following use sensible defaults and don't need to be set:
#   - APP_ENV=local
#   - FRONTEND_PORT=3000
#   - BACKEND_DB_HOST=postgres (Docker container)
#   - LLM uses Ollama in Docker (pull model: make pull-model)
#
# For advanced configuration, see env.template
# =============================================================================
